import * as React from "react"

interface Props {
  bundle: RechargeSubscription[],
}

export default class EditBundle extends React.Component<Props> {
  public render() {
    return (
      <div>edit bundle</div>
    )
  }
}
